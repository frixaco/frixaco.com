[package]
name = "frixa"
version = "0.1.0"
edition = "2024"

[dependencies]
flate2 = { version = "1.1.9", default-features = false, features = ["miniz_oxide"] }
pulldown-cmark = "0.13"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = "symbols"
