{
  "name": "frixaco-vanilla",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "DEV=true bun --hot server.ts",
    "serve": "bun build index.html --minify --outdir=dist && bun build server.ts --minify --outdir=dist --target=bun && bun run --cwd=dist server.js",
    "build": "bun build index.html --minify --outdir=dist"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.19",
    "@types/bun": "latest",
    "bun-plugin-tailwind": "^0.1.2",
    "tailwindcss": "^4.1.15",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@fontsource/ibm-plex-mono": "^5.2.7"
  }
}
